## ----- Admin Chat -----
*:/a [$] = >>>
    if(get_value('packages.AdminChat.Package.Enable')) {
        _adminChat($, player())
    } else {
        msg(concat(_chat('AdminChat', c.'Damn'), 'AdminChat is currently disabled!'))
    }
<<<
