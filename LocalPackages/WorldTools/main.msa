## ----- Get Biome data and teleport to it. ---------------------------------------------
## Semi-requested by Kajar9 [Owner of EST Minecraft Server]
## to fill a pitfall of variable triggers, but i went a little (alot)
## beyond the call of duty. opps :P
*:/getbiome [$xcoord] [$ycoord] [$zcoord] [$world]= >>>
    if(equals($world, '')) {            
        _getBiome($xcoord, $ycoord, $zcoord, trim(ploc(player())[3]), player())
    ,
        _getBiome($xcoord, $ycoord, $zcoord, $world, player())
    }
 <<<
 
 
## ----- Sentinel bespoke region lookup tools --------------------------------------------
## Custom display of region data making it easy to read.
*:/sentinel [$request] [$data] [$flags] [$]= >>>
    switch($request,
        stats,
            _SentinelMain($request, $data, $flags $, player(), ploc(player())[3])
        ,
        info,
            _SentinelMain($request, $data, $flags $, player(), ploc(player())[3])
        ,
        #Case Else:
            _SentinelMain('', null, null, player(), ploc(player())[3])
    )
<<<

